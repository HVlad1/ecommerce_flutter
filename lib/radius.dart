import 'package:flutter/material.dart';

abstract class Radius {
  //Product card
  static BorderRadius radiusProductCard = BorderRadius.circular(15);

  static BorderRadius radiusContaierProducts = BorderRadius.circular(10);

}